import { event, mouse} from 'd3-selection';

import slice from './modules/slice'
import * as utils from './modules/utils'


const d3_slice = {
  slice, utils
}

if (typeof window !== 'undefined') {
  window.d3_slice = d3_slice;
}


export default d3_slice
export {slice, utils}
